# Basic Agent Example

Run:

```sh
python examples/basic_agent/run_example.py
```

The example creates:

- a tool registry,
- tenant and plan policies,
- MCP-server availability checks,
- a fallback tool,
- LangChain-style runtime tool injection,
- LangGraph-style state injection,
- an in-memory audit log.
